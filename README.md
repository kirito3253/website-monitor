# 豊田高専 奨学金情報更新通知ボット (Toyota CT Scholarship Monitor)
豊田工業高等専門学校の「授業料・奨学金」ページを定期的に監視し、更新があった際に LINE で即座に通知する自動化ツールです。

## 開発背景
高専生活において非常に重要な奨学金情報は、更新のタイミングを逃すと申請期限に間に合わないリスクがあります。毎日手動でサイトを確認する手間を省き、情報の更新をいち早くキャッチするために開発しました。

## 使用技術
- **Language:** Python 3.10
- **Libraries:** - `Requests` (Web サイトのデータ取得・LINE API 通信)
  - `BeautifulSoup4` (HTML 解析・特定要素の抽出)
- **Automation:** GitHub Actions (24時間定期実行スケジュール)
- **API:** LINE Messaging API (プッシュ通知)
- **Security:** GitHub Secrets (API キー・ユーザー ID の隠匿)

## 主な機能
1. **特定エリアの監視:** サイト全体の更新ではなく、`<main>` タグ内の主要コンテンツに絞って監視することで、サイドバーの更新などによる誤通知（ノイズ）を防いでいます。
2. **ハッシュ値による比較:** 取得したコンテンツを MD5 ハッシュ値に変換して比較することで、効率的かつ確実に変更を検知します。
3. **完全自動運用:** GitHub Actions を利用し、1日3回（日本時間 9:00, 15:00, 21:00）自動でチェックを行います。
4. **セキュアな設計:** API トークンなどの機密情報は環境変数として管理し、コード内には直接記述していません。

## フォルダ構成
```text
.
├── .github/workflows/
│   └── monitor.yml       # GitHub Actions の実行定義
├── main.py               # 監視・通知ロジック本体
├── requirements.txt      # 依存ライブラリ一覧
├── last_hash.txt         # 前回のサイト状態を記録（自動更新）
└── README.md             # 本ドキュメント
```

## 仕組み (Mechanism)
本システムは、GitHub Actions の仮想環境上で以下のステップを自動実行することで、24時間体制の監視を実現しています。
1.  **トリガー (Trigger)**
    - GitHub Actions が設定されたスケジュール（cron）に基づき、Ubuntu 仮想マシンを起動します。
2.  **データ取得 (Scraping)**
    - Python の `Requests` ライブラリを使用してターゲット URL にアクセスし、HTML データを取得します。
    - `BeautifulSoup4` を用い、ページ内の `<main>` タグに絞ってコンテンツを抽出します。
3.  **状態比較 (Hash Comparison)**
    - 抽出したテキストを `hashlib (MD5)` でハッシュ化し、リポジトリ内の `last_hash.txt` と比較します。
4.  **通知 (Notification)**
    - 前回のハッシュ値と異なる場合、`LINE Messaging API` を通じてプッシュ通知を送信します。
5.  **永続化 (Persistence)**
    - 新しいハッシュ値を `last_hash.txt` に書き込み、GitHub Action ボットがリポジトリへ自動的に `git push` を行い、次回の実行に備えます。

---

## 作者 (Author)

### **kirito3253**
プログラミングの学習に励んでいる学生です。
日々の生活を少し便利にするツールの制作や、新しい技術を学ぶことに楽しみを感じています。

- **GitHub:** [https://github.com/kirito3253](https://github.com/kirito3253)
- **最近の取り組み:**
    - **競技プログラミング:** アルゴリズムの基礎を学ぶため、AtCoderなどの問題演習をコツコツ進めています。
    - **機械学習:** 基礎的な分類モデルの仕組みについて、実際に動かしながら学習しています。
    - **自動化の練習:** 今回のボットのように、自分の身の回りで「あったらいいな」と思うものを形にする練習をしています。

### **学習中の技術 / 興味のあること**
- **言語:** Python, C （基礎から勉強中です）
- **触ってみた技術:** GitHub Actions, LINE Messaging API, BeautifulSoup4, Scikit-learn
- **今の目標:** - 取得したデータをより詳しく分析できるようになること。
    - 他のWebサイトの監視など、少しずつ作れるものの幅を広げていくこと。